<UserControl 
    x:Name="ImageViewElement" 
    x:Class="Imgur.Uwp.Controls.ImageViewElementControl" 
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" 
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" 
    xmlns:local="using:Imgur.Uwp.Controls" 
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
    mc:Ignorable="d" 
    d:DesignHeight="300" 
    d:DesignWidth="400">

    <Grid x:Name="RootMedia">
        <FontIcon 
            FontFamily="{StaticResource SegoeMDLGlyphs}" 
            FontSize="100" 
            Foreground="#3d4140" 
            Glyph="&#xEB9F;"
            HorizontalAlignment="Center"
            VerticalAlignment="Center"/>

        <Rectangle HorizontalAlignment="Stretch" VerticalAlignment="Stretch">
            <Rectangle.Fill>
                <SolidColorBrush Color="Black" Opacity="0.3"/>
            </Rectangle.Fill>
        </Rectangle>

        <Image 
            x:Name="Image" 
            Source="{Binding ImageElement.Link, ElementName=ImageViewElement}" 
            HorizontalAlignment="Center"
            VerticalAlignment="Center"
            Stretch="Uniform" />
    </Grid>
</UserControl>