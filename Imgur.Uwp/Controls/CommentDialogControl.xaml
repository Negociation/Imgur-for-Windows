<UserControl
    x:Class="Imgur.Uwp.Controls.CommentDialogControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Imgur.Uwp.Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:Windows10FallCreatorsUpdate="http://schemas.microsoft.com/winfx/2006/xaml/presentation?IsApiContractPresent(Windows.Foundation.UniversalApiContract, 5)"
    mc:Ignorable="d"
    x:Name="CommentDialogControlRoot"
    d:DesignHeight="300"
    d:DesignWidth="400">


    <Grid >
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="40"/>
        </Grid.RowDefinitions>
        
        <!-- Body -->
        <Grid Grid.Row="0" CornerRadius="5" Background="#202124"
              Margin="5" VerticalAlignment="Stretch" MinHeight="250"
              BorderThickness="1,0,0,0">
            <Grid.RowDefinitions>
                <RowDefinition Height="auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <!-- Post Info -->
            <ContentPresenter Content="{Binding Header, ElementName=CommentDialogControlRoot}"></ContentPresenter>

            <!-- TextBox com limite de 500 chars -->
            <Grid Grid.Row="1" VerticalAlignment="Top" >
                <TextBox 
                     TextWrapping="Wrap"
                     AcceptsReturn="True"
                     Padding="12,10"
                     VerticalAlignment="Top"
                     BorderBrush="Transparent"                    
                     Style="{x:Null}"
                     BorderThickness="0"
                     Background="Transparent"
                     FontWeight="SemiLight"
                     SelectionHighlightColorWhenNotFocused="AliceBlue"
                     FontSize="13"
                     x:Name="CommentBox"
                     Text="{Binding CommentText, Mode=OneWay}"
                     PlaceholderText="{Binding AuthorName, Converter={StaticResource ReplyingStringConverter}}"
                     MaxLength="500"/>
                <Rectangle VerticalAlignment="Bottom" Height="2" Margin="6,-2,6,0"  HorizontalAlignment="Stretch" Stroke="#2fbe7c"/>

            </Grid>


        </Grid>

        <!-- Footer -->
        <Grid Grid.Row="1" Height="40">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="auto"/>
            </Grid.ColumnDefinitions>

            <!-- Botões extras (emoji, imagem) -->
            <StackPanel Grid.Column="0" Orientation="Horizontal">
                <Button Width="50" Height="40" Style="{x:Null}"
                        Padding="0" BorderBrush="Transparent"
                        Background="Transparent">
                    <FontIcon FontSize="20" VerticalAlignment="Center"
                              HorizontalAlignment="Center" Foreground="#B8BCC5"
                              FontFamily="{StaticResource SegoeMDLGlyphs}"
                              Glyph="&#xF4A9;"/>
                </Button>
                <Button Width="50" Height="40" Style="{x:Null}"
                        Padding="0" BorderBrush="Transparent"
                        Background="Transparent">
                    <FontIcon FontSize="20" VerticalAlignment="Center"
                              HorizontalAlignment="Center" Foreground="#B8BCC5"
                              FontFamily="{StaticResource FluentUIGlyphs}"
                              Glyph="&#xE8B9;"/>
                </Button>
            </StackPanel>

            <!-- Contador + Submit -->
            <StackPanel Grid.Column="1" Orientation="Horizontal">
                <!-- Contador de caracteres -->
                <StackPanel Orientation="Horizontal" Margin="5,0">
                    <TextBlock FontSize="10" VerticalAlignment="Center" Margin="0">
                        <Run Foreground="{Binding CanSubmitComment, Converter={StaticResource BoolToBrushConverter}, ConverterParameter=#B8BCC5|#ff4444}" Text="{Binding CommentCharCount}"></Run>
                        <Run  Foreground="#B8BCC5" > / 500</Run>
                    </TextBlock>
                </StackPanel>
                <!-- Submit -->
                <Button Width="50" Height="40"
                        Style="{x:Null}" Padding="0"
                        BorderBrush="Transparent"
                        Background="Transparent"
                        Command="{Binding SubmitCommentCommand}">
                    <FontIcon FontSize="16"
                              VerticalAlignment="Center"
                              HorizontalAlignment="Center"
                              Foreground="{Binding CanSubmitComment,
                                  Converter={StaticResource BoolToBrushConverter},
                                  ConverterParameter=#2fbe7c|#B8BCC5}"
                              FontFamily="{StaticResource FluentUIGlyphs}"
                              Glyph="&#xE724;"/>
                </Button>
            </StackPanel>

        </Grid>

        <Grid Grid.RowSpan="3" Margin="5,120,5,0" Background="#AA202124" CornerRadius="0,0,5,5" Visibility="{Binding SendingComment}">
            <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center" Windows10FallCreatorsUpdate:Spacing="10">
                <ProgressRing Width="40" Height="40" IsActive="True"/>
                <TextBlock Text="Sending..." Foreground="White" FontSize="14" FontWeight="SemiBold" HorizontalAlignment="Center"/>
            </StackPanel>
        </Grid>
    </Grid>
</UserControl>
