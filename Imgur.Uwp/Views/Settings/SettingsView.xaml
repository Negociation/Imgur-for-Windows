<Page
    x:Class="Imgur.Uwp.Views.Settings.SettingsView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Imgur.Uwp.Views.Settings"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:controls="using:Imgur.Uwp.Controls"
    mc:Ignorable="d">
    <Grid x:Name="RootGrid">
        <Grid.Background>
            <LinearGradientBrush StartPoint="0,0" Opacity="0.4"
            EndPoint="0,1">
                <GradientStop Color="#073169" Offset="1" />
                <GradientStop Color="#292c30" Offset="0.6" />
            </LinearGradientBrush>
        </Grid.Background>
        <VisualStateManager.VisualStateGroups>
            <VisualStateGroup  x:Uid="PageTriggers">
                <VisualState x:Name="Minimal">
                    <VisualState.StateTriggers>
                        <AdaptiveTrigger MinWindowWidth="300" />
                    </VisualState.StateTriggers>
                    <VisualState.Setters>
                    </VisualState.Setters>
                </VisualState>
                <VisualState x:Name="Compact">
                    <VisualState.StateTriggers>
                        <AdaptiveTrigger MinWindowWidth="500" />
                    </VisualState.StateTriggers>
                    <VisualState.Setters>
                    </VisualState.Setters>
                </VisualState>
                <VisualState x:Name="Extended">
                    <VisualState.StateTriggers>
                        <AdaptiveTrigger MinWindowWidth="800" />
                    </VisualState.StateTriggers>
                    <VisualState.Setters>
                        <Setter Target="RootGrid.Margin" Value="0,10,0,0" />
                    </VisualState.Setters>
                </VisualState>

                <VisualState x:Name="ExtendedFull">
                    <VisualState.StateTriggers>
                        <AdaptiveTrigger MinWindowWidth="1000" />
                    </VisualState.StateTriggers>
                    <VisualState.Setters>
                        <Setter Target="LeftContent.(Grid.ColumnSpan)" Value="1" />
                        <Setter Target="LeftAbout.Visibility" Value="Visible" />
                        <Setter Target="BottomAbout.Visibility" Value="Collapsed" />
                        <Setter Target="RootGrid.Margin" Value="0,10,0,0" />

                    </VisualState.Setters>
                </VisualState>
            </VisualStateGroup>
        </VisualStateManager.VisualStateGroups>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="0.65*" MaxWidth="600" MinWidth="290" />
            <ColumnDefinition Width="0.4*" />

        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <StackPanel Orientation="Horizontal"  HorizontalAlignment="Stretch" Grid.ColumnSpan="2" Margin="0,5,0,0" Opacity="0.8" CornerRadius="5">
            <Button x:Name="LeaveFloatButton" Command="{Binding LeaveCurrentPage}" VerticalAlignment="Top" Style="{StaticResource CircleButtonStyle}" Margin="5,0,0,0" Width="34" Height="34" Background="Transparent" Opacity="0.9" >
                <FontIcon Visibility="Visible"  Grid.Column="0" FontSize="16" FontWeight="Normal" HorizontalAlignment="Center" VerticalAlignment="Center" FontFamily="{StaticResource FluentUIGlyphs}" Foreground="WhiteSmoke" Glyph="&#xE72B;"/>
            </Button>
            <TextBlock x:Uid="SettingsPageTitle" FontWeight="SemiBold" Margin="5,0,0,0" FontSize="22">App  Settings</TextBlock>
        </StackPanel>
        <ScrollViewer x:Name="LeftContent" Grid.Row="1" Padding="5,0"  HorizontalAlignment="Stretch" Grid.ColumnSpan="2">
            <StackPanel>

                <StackPanel Orientation="Horizontal" Margin="5,20,0,5">
                    <FontIcon FontFamily="{StaticResource FluentUIGlyphs}" FontWeight="Normal" FontSize="18" Margin="0,2,8,0" Glyph="&#xE713;"  HorizontalAlignment="Center" />
                    <TextBlock x:Uid="SettingsPageGeneralTitle" FontWeight="SemiBold" FontSize="17">General Settings</TextBlock>
                </StackPanel>

                <controls:SettingBox x:Uid="SettingsNotification" Margin="5" BoxBrush="#7d54fa" HorizontalAlignment="Stretch"  Header="Remind Notifications" Text="Send notifications sometimes about what's going on imgur community." >
                    <controls:SettingBox.Control>
                        <ToggleSwitch Style="{StaticResource ToggleSwitchStyle}" OnContent="{x:Null}" OffContent="{x:Null}" IsOn="{Binding RemindNotificationsEnabled,Mode=TwoWay}" Margin="0,0,10,0" AutomationProperties.Name="simple ToggleSwitch"/>
                    </controls:SettingBox.Control>
                </controls:SettingBox>

                <controls:SettingBox x:Uid="SettingsData" Margin="5" BoxBrush="#7d54fa"  Header="Data Control" Text="Notify when using mobile data.">
                    <controls:SettingBox.Control>
                        <ToggleSwitch Style="{StaticResource ToggleSwitchStyle}" OnContent="{x:Null}" OffContent="{x:Null}" IsOn="{Binding DataNotificationsEnabled,Mode=TwoWay}"  Margin="0,0,10,0" Grid.Row="0" Grid.RowSpan="2"  Grid.Column="1"  HorizontalAlignment="Right" AutomationProperties.Name="simple ToggleSwitch"/>
                    </controls:SettingBox.Control>
                </controls:SettingBox>

                <controls:SettingBox Margin="5" x:Uid="SettingsMobileUsage" BoxBrush="#7d54fa"  Header="Limit mobile data usage" Text="Only stream HD Videos on Wi-Fi.">
                    <controls:SettingBox.Control>
                        <ToggleSwitch Style="{StaticResource ToggleSwitchStyle}" OnContent="{x:Null}" OffContent="{x:Null}" IsOn="{Binding HDonWifiEnabled,Mode=TwoWay}"  Margin="0,0,10,0" Grid.Row="0" Grid.RowSpan="2"  Grid.Column="1"  HorizontalAlignment="Right" AutomationProperties.Name="simple ToggleSwitch"/>
                    </controls:SettingBox.Control>
                </controls:SettingBox>

                <StackPanel Orientation="Horizontal" Margin="5,20,0,5">
                    <FontIcon FontFamily="{StaticResource FluentUIGlyphs}" FontWeight="Normal" FontSize="18" Margin="0,2,8,0" Glyph="&#xE790;"  HorizontalAlignment="Center" />
                    <TextBlock x:Uid="SettingsPageAppearanceTitle"  FontWeight="SemiBold" FontSize="17">Appearance</TextBlock>
                </StackPanel>
                <controls:SettingBox  Margin="5" x:Uid="SettingsThumbnail" BoxBrush="#7d54fa"  Header="Thumbnail Size" Text="Size of galleries thumbnails.">
                    <controls:SettingBox.Control>
                        <ComboBox Margin="10" SelectedIndex="{Binding ThumbnailsSize,Mode=TwoWay}" >

                            <ComboBoxItem x:Uid="SettingsThumbnailSmall" />
                            <ComboBoxItem x:Uid="SettingsThumbnailMedium" />
                            <ComboBoxItem x:Uid="SettingsThumbnailLarge" />
                        </ComboBox>
                    </controls:SettingBox.Control>
                </controls:SettingBox>

                <controls:SettingBox x:Uid="SettingsAutoPlay" Margin="5" BoxBrush="#7d54fa" Header="AutoPlay das Miniaturas" Text="Conteudos de video das miniaturas dão auto-play automaticamente.">
                    <controls:SettingBox.Control>
                        <ToggleSwitch Style="{StaticResource ToggleSwitchStyle}" OnContent="{x:Null}" OffContent="{x:Null}" IsOn="{Binding AutoPlayEnabled,Mode=TwoWay}"  Margin="0,0,10,0" Grid.Row="0" Grid.RowSpan="2"  Grid.Column="1"  HorizontalAlignment="Right" AutomationProperties.Name="simple ToggleSwitch"/>
                    </controls:SettingBox.Control>
                </controls:SettingBox>

                <controls:SettingBox x:Uid="SettingsLiveTiles" Margin="5" BoxBrush="#7d54fa"   Header="Use Live Tiles" Text="Show Live tiles with viral content.">
                    <controls:SettingBox.Control>
                        <ToggleSwitch Style="{StaticResource ToggleSwitchStyle}" OnContent="{x:Null}" OffContent="{x:Null}" IsOn="{Binding LiveTilesEnabled,Mode=TwoWay}"  Margin="0,0,10,0" Grid.Row="0" Grid.RowSpan="2"  Grid.Column="1"  HorizontalAlignment="Right" AutomationProperties.Name="simple ToggleSwitch"/>
                    </controls:SettingBox.Control>
                </controls:SettingBox>
                <controls:AboutControl x:Name="BottomAbout"  HorizontalAlignment="Left" Margin="5,0,10,20" />
            </StackPanel>
        </ScrollViewer>

        <controls:AboutControl x:Name="LeftAbout"  HorizontalAlignment="Right" Margin="10,39,20,0" MaxWidth="350"   Grid.Column="1" Grid.RowSpan="2" Visibility="Collapsed"/>


    </Grid>
</Page>
