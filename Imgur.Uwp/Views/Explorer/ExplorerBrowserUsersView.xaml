<Page
    x:Class="Imgur.Uwp.Views.Explorer.ExplorerBrowserUsersView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Imgur.Uwp.Views.Explorer"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:toolkit="using:Microsoft.Toolkit.Uwp.UI.Controls"
    xmlns:controls="using:Imgur.Uwp.Controls"
    xmlns:accountvm="using:Imgur.ViewModels.Account"
    mc:Ignorable="d">
    <Grid>

        <!-- BACKGROUND -->
        <Grid.Background>
            <LinearGradientBrush StartPoint="0,0" EndPoint="0,1" Opacity="0.4">
                <GradientStop Color="#292c30" Offset="0.6"/>
                <GradientStop Color="#073169" Offset="1"/>
            </LinearGradientBrush>
        </Grid.Background>

        <VisualStateManager.VisualStateGroups>
            <VisualStateGroup x:Uid="PageTriggers">
                <VisualState x:Name="Extended2">
                    <VisualState.StateTriggers>
                        <AdaptiveTrigger MinWindowWidth="500" />
                    </VisualState.StateTriggers>
                    <VisualState.Setters>
                        <Setter Target="SearchUsersRepeaterControl.ItemHeight" Value="200" />
                        <Setter Target="SearchUsersRepeaterControl.StretchContentForSingleRow" Value="False" />
                        <Setter Target="SearchUsersRepeaterControl.DesiredWidth" Value="220" />
                    </VisualState.Setters>
                </VisualState>
            </VisualStateGroup>
        </VisualStateManager.VisualStateGroups>
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="auto"></RowDefinition>
                <RowDefinition Height="*"></RowDefinition>
            </Grid.RowDefinitions>
            
            <!-- HEADER -->
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Stretch" Margin="0,5,0,10" Grid.Row="0" Opacity="0.8">
                <Button Command="{Binding LeaveCurrentPage}" VerticalAlignment="Top" Style="{StaticResource CircleButtonStyle}" Margin="5,0,0,0" Width="34" Height="34" Background="Transparent" Opacity="0.9">
                    <FontIcon Visibility="Visible" Grid.Column="0" FontSize="16" FontWeight="Normal" HorizontalAlignment="Center" VerticalAlignment="Center" FontFamily="{StaticResource FluentUIGlyphs}" Foreground="WhiteSmoke" Glyph="&#xE72B;"/>
                </Button>
                <TextBlock  FontWeight="SemiBold" Margin="5,0,0,0" FontSize="22">
                        <Run x:Uid="UsersSection"></Run>
                        <Run>: </Run>
                        <Run Text="{Binding Query}"></Run>
                </TextBlock>
            </StackPanel>
            <toolkit:AdaptiveGridView
                            x:Name="SearchUsersRepeaterControl"
                            Grid.Row="1"
                            IncrementalLoadingThreshold="0.1"
                            DataFetchSize="1"
                            IncrementalLoadingTrigger="Edge"
                            DesiredWidth="500"
                            ItemHeight="80"
                            OneRowModeEnabled="False"
                            StretchContentForSingleRow="True"
                            ItemsSource="{Binding UsersVmCollection, Mode=OneWay}"
                            IsItemClickEnabled="True"
                            SelectionMode="None">
                <toolkit:AdaptiveGridView.ItemTemplate>
                    <DataTemplate x:Name="SearchUsersItemTemplate" x:DataType="accountvm:AccountViewModel">
                        <controls:AccountTileControl ViewModel="{Binding}" EnableCompactMode="True"/>
                    </DataTemplate>
                </toolkit:AdaptiveGridView.ItemTemplate>
                <toolkit:AdaptiveGridView.ItemContainerStyle>
                    <Style TargetType="GridViewItem">
                        <Setter Property="VerticalContentAlignment" Value="Stretch"/>
                        <Setter Property="Margin" Value="0,1"/>
                        <Setter Property="Padding" Value="0"/>
                        <Setter Property="Width" Value="220"/>
                        <Setter Property="Height" Value="200"/>
                    </Style>
                </toolkit:AdaptiveGridView.ItemContainerStyle>
            </toolkit:AdaptiveGridView>
        </Grid>
    </Grid>
</Page>
